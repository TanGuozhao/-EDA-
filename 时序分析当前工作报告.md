# 时序分析当前工作报告

更新时间：2026-08-02

## 1. 目标与范围

当前完成的是 EDA 教学闯关平台中“时序分析和电气验证”章节的首个时序分析关卡。关卡以一张横向 DAG 为核心，玩家围绕同一张图完成五道题：

1. 计算到达时间
2. 计算要求时间
3. 计算裕量
4. 计算给定信号路径的总耗时
5. 选择总延迟最小的路径

大模型只负责生成 DAG。题目元数据由后端固定规则构造；到达时间、要求时间、裕量、路径总耗时和最短路径均由后端时序引擎计算与验证。模型不会生成或返回答案。

## 2. 当前关卡规则

### 2.1 单图五题

- 每个挑战只生成一张 DAG。
- 同一张图一次性派生五道题。
- 前端在本地切换五道题，不会在切题时再次调用模型。
- 挑战 ID 相同的五道题通过各自的 `question_id` 提交与验证。

### 2.2 固定复杂度

时序分析不再使用 `difficulty` 参数。所有新生成 DAG 必须满足固定结构：

| 约束 | 当前规定 |
| --- | --- |
| 非终端节点数 | 12-15 个 |
| 分支点 | 至少 3 个 |
| 汇合点 | 至少 3 个 |
| 最长路径 | 至少 7 个非终端节点 |
| START 到 END 路径数 | 至少 3 条 |
| 到达时间题 | 4 个填空节点 |
| 要求时间题 | 4 个填空节点 |
| 裕量题 | 4 个填空节点 |
| 路径总耗时题 | 至少 7 个非终端节点 |

模型提示与后端解析后校验均执行这些规则。不合格 DAG 不会进入题目流程。

## 3. 前端实现

正式入口：

```text
/chapter/5/timing-analysis
```

主要组件：

| 文件 | 职责 |
| --- | --- |
| `src/views/BlankTimingAnalysis.vue` | 获取挑战、切换五题、构造提交载荷、选择路径 |
| `src/components/HorizontalTimingGraph.vue` | 左侧可点击横向 DAG、入边与已选路径高亮 |
| `src/components/PathNodeMap.vue` | 右侧路径映射图 |
| `src/components/NodeBlankList.vue` | 计算题通用节点填空模块 |
| `src/components/TimingTaskCard.vue` | 题目、作答区、提交三类卡片容器 |

### 3.1 路径题交互

- 玩家在左侧主 DAG 上按连线顺序逐节点选择路径。
- 已选择的节点与边会在左图和右侧映射图同步高亮。
- 点击不连续节点不会清空已选路径，而是提示该节点不是当前路径的下一节点。
- 右侧映射图仅用于展示当前选择，不承担节点选择操作。

### 3.2 右侧映射图布局

- 映射图填满作答卡片的可用高度。
- SVG 按其完整横向 DAG 比例渲染。
- 映射容器提供横向滚动条，宽图可以查看完整映射，不再压缩成不可读的缩略图。

### 3.3 前后端地址

前端 API 已从固定地址改为同源相对路径：

```javascript
const BASE = import.meta.env.VITE_API_BASE ?? ''
```

开发环境由 Vite 将 `/api` 代理到本机 `127.0.0.1:8000`；生产环境由 Nginx 将 `/api` 转发到后端本机端口 `8000`。浏览器不会再错误请求访问者电脑上的 `127.0.0.1:8000`。

## 4. 后端实现

主要模块：

| 文件 | 职责 |
| --- | --- |
| `backend/app/timing/agent.py` | 调用 LLM 生成 DAG、校验固定复杂度、派生五道题定义 |
| `backend/app/timing/dag_text.py` | 解析 DAG 文本格式 |
| `backend/app/timing/analysis.py` | 到达时间、要求时间、裕量、最短路径、路径总耗时计算 |
| `backend/app/api/routers/timing_challenges.py` | 生成、读取、验证挑战的 API |
| `backend/scripts/timing_challenge_cli.py` | 命令行答题客户端 |

### 4.1 DAG 文本格式

```text
START-A
A-C
C-END
A:2
C:4
```

- 连线使用 `源节点-目标节点`。
- 节点延迟使用 `节点:延迟`。
- `START` 与 `END` 延迟固定为 0。

### 4.2 API

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/timing-analysis/challenges/generate` | 生成一张 DAG 和五道题 |
| `GET` | `/api/timing-analysis/challenges/{challenge_id}` | 读取当前进程内挑战 |
| `POST` | `/api/timing-analysis/challenges/{challenge_id}/validate` | 验证某一道题的作答 |

生成请求当前仅支持：

```json
{
  "topic": "时序分析 DAG 练习"
}
```

计算题提交示例：

```json
{
  "question_id": "arrival_time",
  "answers": {"A": 2, "B": 5, "C": 7, "D": 9}
}
```

最短路径提交示例：

```json
{
  "question_id": "shortest_path",
  "path": ["START", "A", "C", "END"]
}
```

## 5. 服务器部署状态

当前服务器部署包含：

| 项目 | 状态 |
| --- | --- |
| Nginx 静态前端 | 已部署，监听 80 端口 |
| Nginx `/api` 反向代理 | 已部署，转发到 `127.0.0.1:8000` |
| FastAPI 后端 | 已部署 |
| systemd 服务 | `chip-platform-api.service` 已启用并开机自启 |
| 数据库 | SQLite，本地文件 `eda_platform.db` |
| 模型服务 | 已配置为外部 OpenAI 兼容接口 |

超时配置：

| 位置 | 当前值 |
| --- | --- |
| Nginx `proxy_read_timeout` | 180 秒 |
| Nginx `proxy_send_timeout` | 180 秒 |
| 后端 `LLM_TIMEOUT_SECONDS` | 150 秒 |

模型调用、数据库 URL、API Key 等敏感环境变量只保存在服务器 `backend/.env`，不应提交到 Git 仓库。

## 6. 已完成验证

| 验证项 | 结果 |
| --- | --- |
| 后端时序相关单测 | 8 项通过 |
| 前端生产构建 | 通过 |
| 五题 UI 渲染、提交载荷 | 浏览器模拟通过 |
| 单图五题生成接口调用次数 | 浏览器模拟中为 1 次 |
| 最短路径逐节点点击 | 浏览器模拟通过 |
| 右侧映射横向滚动 | 浏览器模拟通过 |
| 真实模型固定结构生成 | 通过：15 个非终端节点、3 分支点、3 汇合点、五种题型齐全 |
| 服务器内部页面与 API | 均返回 200 |

真实模型单次生成在服务端可能耗时约 50-130 秒。耗时来自模型服务与排队，不是 DAG 解析或时序计算。

## 7. 当前边界与风险

1. 挑战目前只保存在后端进程内存中。
   - 服务重启后，已生成挑战无法再读取或验证。
   - 每次新的生成请求都会调用模型，题目可能不同。

2. 当前没有 Redis 缓存。
   - Redis/SQLite 持久化草稿已按要求撤销，尚未部署。

3. 当前没有真实用户身份。
   - 无法按玩家维度持久化作答历史、题目去重或学习进度。

4. 模型调用仍是同步请求。
   - Nginx 已避免 60 秒默认超时，但首次生成仍需要等待模型返回。

## 8. 已讨论、未实施的缓存方案

建议采用“SQLite 持久化 + Redis 缓存 + 后台预生成”三层结构：

```text
SQLite：挑战、历史、作答记录的唯一事实来源
Redis：当前关卡指针、挑战热缓存、生成锁、任务进度
后台生成器：唯一调用 LLM 的位置
```

目标流程：

1. 玩家读取当前已发布挑战，不在请求链路调用 LLM。
2. 新题由后台预生成、固定规则校验、去重后写入题库。
3. 发布时原子切换当前挑战 ID；旧题永久保留，可回滚。
4. Redis 故障时从 SQLite 恢复；SQLite 才是持久化来源。
5. 后续接入用户身份后，增加按 `user_id + challenge_id + question_id` 的作答记录。

Redis 已可单独安装，但在确认数据模型、共享范围和“新一关”发布流程前，不应先行接入运行代码。

## 9. 后续决策项

1. 当前关卡是否所有玩家共用一张图，还是按玩家分配不同图。
2. 谁可以触发“生成下一关”和“发布下一关”。
3. 题库中应预生成多少张备用 DAG。
4. 是否先采用匿名玩家 ID，或直接接入账号系统。
5. 是否在生成任务中加入模型失败重试和人工审核。
