# 芯片平台服务器接入

服务器：`root@47.113.201.204`

这份文档写给**你**：需要从自己的 Windows 电脑登录服务器的人。

文中只有另外一个人：**服务器管理员**。服务器管理员已经能以 `root` 身份登录 `47.113.201.204`，并负责把你的公钥加入服务器。AI 在你的电脑上运行，并使用你已经加载到 `ssh-agent` 的密钥登录服务器。

## 第一步：你在自己的 Windows 电脑生成密钥

你打开 PowerShell，执行：

```powershell
ssh-keygen -t ed25519 -C "chip-platform-deploy" -f $HOME\.ssh\chip_platform_server
Get-Content $HOME\.ssh\chip_platform_server.pub
```

第二条命令会显示一整行以 `ssh-ed25519` 开头的文本。

**你将这一整行文本发送给服务器管理员。**

## 第二步：服务器管理员把队友的公钥加入服务器

服务器管理员先登录服务器：

```powershell
ssh root@47.113.201.204
```

服务器管理员进入 Linux 服务器后，执行以下命令。第二行单引号中的内容，替换为你在第一步发送的整行公钥：

```bash
mkdir -p /root/.ssh
chmod 700 /root/.ssh
echo '你在第一步发送的整行 ssh-ed25519 公钥粘贴在这里' >> /root/.ssh/authorized_keys
chmod 600 /root/.ssh/authorized_keys
```

执行完成后，服务器管理员回复你：“公钥已加入服务器”。

## 第三步：你把服务器登录密码保存到自己的电脑

你在自己的密码管理器中新建一条记录：

```text
名称：芯片平台服务器
地址：47.113.201.204
用户名：root
密码：服务器管理员提供的 root 登录密码
```

这份密码仅用于密钥失效时的应急登录。不要写入代码、`.env`、PowerShell 脚本、SSH 配置文件或聊天消息。

## 第四步：你配置免密登录

你以管理员身份打开 PowerShell，执行：

```powershell
Set-Service -Name ssh-agent -StartupType Automatic
Start-Service ssh-agent
```

你关闭管理员 PowerShell，重新打开普通 PowerShell，执行：

```powershell
@'
Host chip-platform
    HostName 47.113.201.204
    User root
    IdentityFile ~/.ssh/chip_platform_server
    IdentitiesOnly yes
'@ | Add-Content $HOME\.ssh\config

ssh-add $HOME\.ssh\chip_platform_server
ssh-add -l
ssh chip-platform
```

最后一条命令直接进入服务器时，免密登录配置完成。

## 第五步：你让 AI 登录服务器

你必须在完成第四步、并确认 `ssh chip-platform` 可以登录后，才向 AI 发送下面这句话：

```text
我的电脑已经配置并加载了 SSH 密钥。请使用 `ssh chip-platform` 登录 `root@47.113.201.204`。
```

AI 可执行：

```powershell
ssh chip-platform "systemctl status chip-platform-api.service"
```

你不要把服务器密码、私钥文件内容或密钥口令发送给 AI。
