# 逻辑设计章节任务清单

## 任务一：RTL 设计后端接口

### 目标

建立一组仅由后端执行的 RTL 设计接口。接口根据自然语言需求生成 Verilog，调用部署机器本地的 Yosys 进行语法、层次与综合检查，并生成、判定 Verilog 代码改错题。

前端只展示题目、编辑 Verilog、提交原始代码与展示后端结果，不执行 Verilog 解析、综合、仿真或判题。

### 范围

1. 根据自然语言需求生成参考 Verilog。
2. 对 Verilog 执行本地工具验证。
3. 生成 Verilog 代码改错题。
4. 判定玩家修正后的 Verilog。
5. 持久化设计、题目、工具报告与玩家提交记录。

### 接口

| 接口 | 用途 | 输入 | 输出 |
|---|---|---|---|
| `POST /api/rtl/generate` | 根据文字需求生成 RTL 设计产物 | 需求描述、模块名、端口约束、可选生成模式 | `design_id`、文字需求、参考 Verilog、端口定义、生成记录 |
| `POST /api/rtl/validate` | 验证一份 Verilog | Verilog、顶层模块名、可选参考设计/隐藏测试集 | 编译结果、Yosys 综合结果、功能测试结果、结构告警 |
| `POST /api/rtl/questions/repair` | 生成 Verilog 改错题 | 文字需求、模块约束、错误类型配置 | `question_id`、学生侧题干、错误 Verilog、提交规则 |
| `GET /api/rtl/questions/{question_id}` | 获取学生侧改错题 | `question_id` | 文字需求、错误 Verilog、允许端口、提交规则 |
| `POST /api/rtl/questions/{question_id}/submit` | 判定玩家修正结果 | 玩家修正后的 Verilog | 编译/综合/功能判定、反馈、得分、提交记录 |
| `GET /api/rtl/designs/{design_id}/artifacts` | 获取设计过程产物 | `design_id` | 参考 RTL、网表、工具日志、综合报告、测试报告 |

### RTL 生成与改错题生成链

```text
文字需求
  -> LLM 生成参考 Verilog
  -> 后端编译、仿真、Yosys 综合验证
  -> 受控错误注入，得到错误 Verilog
  -> 再次验证错误 RTL 确实不能通过目标检查
  -> 保存隐藏参考 RTL、隐藏测试集、学生侧错误 RTL 和题干
```

`POST /api/rtl/questions/repair` 调用 RTL 生成服务取得文字需求与正确 RTL。错误 RTL 不应只依赖大模型随机生成；应优先由后端在正确 RTL 上注入可追踪错误，再用工具链验证。这样可以保证题目存在真实错误，正确版本可通过检查，且反馈能对应明确错误类型。

第一批受控错误类型：

| 错误类型 | 示例 |
|---|---|
| 时序赋值错误 | 在时序逻辑中错误使用 `=` 或阻塞/非阻塞赋值混用 |
| 不完整组合逻辑 | 缺少 `else` 或 `default`，导致 latch 推断 |
| 位宽与符号错误 | 截断、零扩展/符号扩展错误、signed/unsigned 比较错误 |
| 状态机错误 | 复位状态、状态转移条件或输出状态错误 |
| 条件与运算符错误 | 比较符号、逻辑运算符、位运算符错误 |
| 端口与信号错误 | 漏驱动、错误连接、端口方向错误 |
| 时钟使能错误 | Enable 条件反置或遗漏 |

### 验证职责

Yosys 不能单独证明功能正确。它应承担：

1. Verilog 解析与层次展开。
2. `proc`、`check` 等结构检查。
3. 逻辑综合与门级统计。
4. 结构问题报告，例如未驱动信号、组合环、latch 推断或无法综合的结构。

功能正确性必须额外通过以下至少一种方式判断：

1. 使用 Icarus Verilog 或 Verilator 运行隐藏测试平台与测试向量。
2. 对受限的组合逻辑题，比较参考 RTL 与玩家 RTL 的全部输入输出。
3. 对适合的场景，使用 Yosys 等价检查比较参考设计与玩家设计。

最终判题原则：只有通过编译、Yosys 综合检查和功能测试的玩家 RTL，才判定为正确。

### 后端执行约束

1. Yosys、Icarus Verilog/Verilator 必须在后端本机或服务器本机执行，不由浏览器调用。
2. 每次验证创建隔离工作目录，只写入本次 RTL、测试平台和工具输出。
3. 工具进程必须设置超时、内存限制、CPU 限制与无网络权限。
4. 不执行玩家提供的脚本、Makefile、Tcl 或任意 shell 命令；后端只生成固定工具命令。
5. 工具原始日志、命令版本、输入文件哈希与结果摘要需要持久化，便于复现判题。

### 持久化对象

| 对象 | 必须保存的内容 |
|---|---|
| RTL 设计 | `design_id`、文字需求、参考 RTL、顶层模块、端口、LLM 模型与生成时间 |
| 验证运行 | 输入 RTL 哈希、工具版本、命令摘要、编译日志、Yosys 日志、网表统计、功能测试结果 |
| 改错题 | `question_id`、关联 `design_id`、学生侧错误 RTL、错误类型、隐藏答案、隐藏测试集、状态 |
| 玩家提交 | 玩家标识、题目、提交 RTL、验证运行、结果、得分、提交时间 |

### 验收标准

1. 后端可根据一段自然语言需求生成一个可综合的参考 Verilog 模块。
2. 参考 Verilog 能通过本地 Yosys 检查与至少一组功能测试。
3. 改错题接口不会向学生侧响应暴露正确 RTL、隐藏测试集或错误位置。
4. 错误 RTL 至少不能通过功能测试、结构检查或两者之一；正确 RTL 必须通过全部判定。
5. 玩家提交后，后端返回明确的编译、综合和功能测试状态。
6. 在服务器部署时，验证实际调用服务器本地 Yosys 与仿真工具。

## 任务二：高级综合第一关

### 这关要教什么

高级综合的任务，是把“哪些操作必须先后执行”的计算图，变成一张真正可以按时钟周期执行的计划表。学生要看懂：同一张 DAG，在追求最短执行时间、给定截止时间，或者运算单元数量有限时，会得到不同的排法；性能、功耗和面积之间也不存在永远最好的单一方案。

本关直接使用已经完成的 DAG 数据结构。一个节点代表一次操作，例如加法、乘法、比较或数据选择；一条箭头代表“后面的操作必须等前面的结果出来才能开始”。

### 学生如何完成一题

进入关卡后，页面左侧显示一张尚未调度的操作 DAG。纵向的每一条线表示一个时钟周期，横向位置表示这一周期中使用的第几个运算单元位置。右侧用一句话给出本题目标和约束。

学生把左侧节点拖到周期线上，并重新连接箭头，画出自己认为正确的执行计划。例如：把没有前驱的加法放到第 0 个周期；把依赖它结果的比较操作放到后续周期；当本题只提供一个加法器时，两个加法不能放在同一周期的同一个资源位置。提交后，系统告诉学生哪些节点放对了、哪些依赖边遗漏或连错了、哪些周期或资源位置需要调整。

本关包含两类练习：

| 练习 | 学生看到的内容 | 学生要完成的事 |
|---|---|---|
| PPA 取舍：柏拉图优化 | 4 到 6 个候选实现方案。每个方案给出性能、功耗、面积/成本数据和题目约束。 | 找出“再提高任一指标都会让至少一个其他指标变差”的方案，选出全部柏拉图最优方案。 |
| 操作调度 | 一张操作 DAG、指定的调度算法和资源条件。 | 通过拖动节点和连接箭头，完成这张 DAG 的周期排布。 |

操作调度题分为三种明确任务：

| 算法 | 题目会怎么说 | 学生画出的结果 |
|---|---|---|
| ASAP | “请让每个操作尽可能早开始。” | 每个节点都放在满足依赖关系的最早周期。 |
| ALAP | “总共只能使用 N 个周期，请让每个操作尽可能晚开始。” | 每个节点都放在不超过总周期、同时满足依赖关系的最晚周期。 |
| List Scheduling | “给定 1 个加法器、1 个乘法器……请排出执行计划。” | 每个节点既满足前后依赖，也不超过该周期可用的同类运算单元数量。 |

### 出题和判题方式

LLM 负责出题素材：生成原始 DAG、节点操作类型、题干和公开约束。后端先检查这张图确实无环、节点和边完整、题目条件足够求解，再放入题库。

标准答案由后端算法计算。ASAP、ALAP 和 List Scheduling 都由后端在同一份原始 DAG 上运行；柏拉图题由后端比较所有候选方案的性能、功耗、面积/成本与硬约束。前端只负责显示、拖拽、连线和提交学生的操作结果。

第一关先完成 ASAP、ALAP、List Scheduling 和柏拉图优化。HU、ILP、兼容图、冲突图和资源绑定会在后续高级综合关卡展开。

### 需要提供的接口

| 接口 | 给谁用 | 作用 |
|---|---|---|
| `GET /api/hls/challenges/current` | 前端进入关卡时调用 | 返回学生当前要做的题：原始 DAG、题干、资源条件、候选方案和初始画布布局。 |
| `POST /api/hls/challenges/generate` | 管理端或后台任务调用 | 调用 LLM 生成一题，完成 DAG 和约束校验后加入题库。 |
| `GET /api/hls/challenges/{challenge_id}` | 前端刷新或恢复会话时调用 | 重新取得同一道题，保证刷新页面后题目不变。 |
| `POST /api/hls/challenges/{challenge_id}/submit` | 学生点击提交时调用 | 接收学生画出的节点位置、箭头连接，或柏拉图题的方案选择；返回得分和具体反馈。 |

### 完成标准

1. 学生能从首页进入 `/chapter/2/high-level-synthesis`，看到一张可拖动、可连线的操作 DAG 和明确题干。
2. 系统能够生成并验证 ASAP、ALAP、List Scheduling、柏拉图优化四类题目。
3. 学生提交后，页面能指出具体错误：节点周期不对、资源超用、依赖边漏连/连错，或柏拉图方案漏选/误选。
4. 刷新页面后，学生仍拿到同一道未完成题；判题和答案计算都由后端完成。

## 任务三：首页 EDA 智能对话、文件问答与 RAG

### 目标

在首页建立一个独立、可持续使用的 EDA 智能对话界面。它不是悬浮提示框，而是包含会话列表、历史消息、流式回复、附件、引用来源和取消生成能力的主工作区。后端负责上下文构造、文件解析、检索、Skill/Tool 调用、权限校验、审计和模型调用；前端只展示会话、上传文件、发送用户输入并渲染 SSE 事件。

对话上下文、附件处理和 RAG 的行为以 `F:\ITRAP` 当前实现为基准。ITRAP 本身没有通用的 Skill 注册、MCP 或 OpenAI tool/function-call 调度框架，因此本项目需要新增明确的 Skill/Tool 契约；该新增能力不得改变 ITRAP 已有的会话与 RAG 语义。

### ITRAP 对话上下文基线

1. 用户消息与助手消息按会话持久化；新会话以最后一条用户消息的前 80 个字符生成标题，已有会话更新最后活动时间。
2. 续聊不将完整历史无限拼入模型。后端从持久化记录读取最近 5 轮的“用户问题 + 助手回复摘要”，构造多轮记忆块；当前用户消息、附件上下文和 RAG 上下文再拼入本轮 system/user payload。
3. 模型回复必须产生一段仅供后续对话使用的摘要。流式输出时，正文以 SSE `delta` 发送；摘要标记块不向用户显示；结束时以 SSE `done` 返回 `session_id` 和本轮 RAG 来源。取消接口按 `request_id + user_id` 终止流，避免跨用户取消。
4. 每次流式请求使用独立数据库会话写入助手消息，处理连接跨请求生命周期的问题；如果新会话在流结束前异常丢失，按 ITRAP 的一次性恢复逻辑重建会话与已写入的用户消息后再落库。
5. 助手消息需保存正文、回复摘要、生成时间、模型标识、Skill/Tool 调用记录和本轮 `rag_sources`；会话、消息、附件、知识库文档和索引任务都必须按用户或知识库空间隔离。

### 文件与附件

| 文件类型 | 支持方式 | 处理上限与降级 |
|---|---|---|
| PDF | PyMuPDF 提取文本 | 最多读取 50 页；未提取到有效文字时，对前 8 页执行 OCR |
| DOCX | `python-docx` 提取段落与表格 | 保持文档块顺序，表格行以制表符连接 |
| TXT | 按 UTF-8、UTF-8 BOM、GBK、GB18030 依次解码 | 失败时以替换字符读取 |
| JPG/JPEG/PNG/WEBP/GIF/BMP | OCR 提取文字 | 复用统一 OCR 服务 |
| DOC | 不支持 | 明确提示用户另存为 DOCX 或 PDF，不能静默失败 |

单次附件解析文件大小上限为 20MB；解析文本上限为 60,000 字符。对话请求最多携带 6 个附件。进入模型的附件上下文总预算为 18,000 字符，单附件按剩余预算截断到 1,200 至 6,000 字符，并明确标记截断。附件需先通过独立上传/解析接口获得 `attachment_id`，会话消息只引用该 ID，不能把二进制文件或全文重复写入消息表。

### RAG 设计

1. 用户可将已解析文件导入个人或课程知识库；后端保存原文件元数据、解析文本、文档哈希、解析状态、分块记录和索引版本。重复文件以哈希去重，重新上传同一文档时可复用解析或覆盖指定版本。
2. 分块以结构边界优先：标题、段落、表格和代码块不被无意义截断；每个块保存 `chunk_id`、`document_id`、原文、用于检索的文本、展示文本、页码/段落/标题路径和字符区间。索引构建时每个检索文本最多使用前 420 字符。
3. embedding 与 ITRAP 一致：使用 SiliconFlow 的 `BAAI/bge-large-zh-v1.5`，维度 1024；调用 `/embeddings` 时单批 1 条、单输入最多 1,200 字符，得到向量后做 L2 归一化。
4. 向量索引使用 FAISS `IndexFlatIP`，持久化 `rag_chunks.faiss` 与按向量顺序对应的 `rag_chunks_ids.json`。新增或更新文档时只重嵌入该文档的块，移除旧 chunk 后重建最终索引，不能全量丢失其他文档向量。
5. 问答时使用当前用户问题检索 Top-4；每个引用展示文本最多 900 字符。检索块与多轮记忆共同放入 system 上下文，并要求模型在材料不足时明确说明，不得编造来源。SSE `done` 和持久化助手消息都返回结构化来源、分数、文档 ID 与定位信息。

### Skill 与 Tool 契约

1. `Skill` 是可配置的 EDA 专业能力包，定义标识、名称、版本、适用章节、系统提示补充、允许的 Tool、输入/输出 JSON Schema、是否可见、权限级别和最大上下文预算。Skill 只改变提示、可用工具与结果呈现规则，不能自行执行系统命令。
2. `Tool` 是后端受控能力，至少包括：知识库检索、已上传文件读取、时序 DAG 分析、高级综合调度校验、RTL/Yosys 验证和课程进度查询。每个 Tool 必须声明 JSON Schema、超时、最大输出、是否需要登录、允许调用它的 Skill，以及可审计的版本号。
3. 模型不得直接执行 shell、SQL、文件路径或网络请求。所有工具调用必须经过后端的 allowlist、Schema 验证、用户/会话/资源归属检查、超时和输出截断；工具结果作为带来源标记的上下文返回模型。高风险工具必须显式要求用户确认。
4. 每轮消息记录 `tool_calls`、参数摘要、结果摘要、耗时、错误状态和关联资源 ID；前端只展示允许公开的调用摘要与引用，不能得到隐藏系统提示、密钥、任意文件路径或未授权工具输出。

### Codex Skill 文件结构与导入测试

任务三的 Skill 实现以项目根目录下 `codex-skill-samples/openai-docs` 为标准样例。该样例复制自 Codex 系统内置 Skill，并已通过 `skill-creator/scripts/quick_validate.py` 校验。实现时需要支持同等结构的 Skill 包导入，而不是只支持数据库里手写的 Skill 配置。

标准 Skill 目录结构如下：

```text
skill-name/
  SKILL.md              # 必需；包含 YAML frontmatter 和 Markdown 使用说明
  agents/
    openai.yaml         # 推荐；提供界面展示名、短描述、默认提示等 UI 元数据
  scripts/              # 可选；放置可由后端受控调用或用于准备上下文的脚本
  references/           # 可选；放置按需加载的长文档、规则、术语表或样例
  assets/               # 可选；放置图标、模板、示例资源等非上下文材料
```

`SKILL.md` 的 YAML frontmatter 至少包含：

```yaml
---
name: "skill-name"
description: "说明该 Skill 能做什么，以及哪些用户请求会触发它。"
---
```

后端导入 Skill 时必须完成以下处理：

1. 扫描指定 Skill 根目录，识别每个包含 `SKILL.md` 的一级目录为一个 Skill。
2. 校验 `SKILL.md` frontmatter 中的 `name` 与 `description`，并校验目录名与 `name` 一致。
3. 读取 `agents/openai.yaml` 作为可选 UI 元数据；缺失时仍允许导入，但必须有默认展示值。
4. 只把 `SKILL.md` 的元数据暴露给 Skill 列表接口；只有当用户显式选择或模型路由命中该 Skill 时，才把 `SKILL.md` 正文和必要的 `references/` 内容加入模型上下文。
5. `scripts/` 中的脚本不得被模型直接执行；如需执行，必须映射为受控 Tool，并经过任务三规定的 allowlist、Schema、权限、超时和审计流程。
6. 导入失败、结构缺失、frontmatter 非法、名称冲突、权限不足等情况必须返回明确错误并写入审计日志。

任务三的实现标准是：能够导入 `codex-skill-samples/openai-docs` 这个样例 Skill；`GET /api/skills` 能列出它；用户在首页对话中选择或触发该 Skill 后，后端能把该 Skill 的指令注入模型上下文，并让大模型按该 Skill 的说明完成一次可观察的测试回复，同时记录本轮使用的 `skill_id`、上下文来源和可公开的调用摘要。

### 接口

| 接口 | 用途 |
|---|---|
| `POST /api/chat/sessions`、`GET /api/chat/sessions`、`GET /api/chat/sessions/{id}/messages`、`DELETE /api/chat/sessions/{id}` | 创建、列出、读取和删除用户自己的会话 |
| `POST /api/chat/stream` | 接收 `session_id`、当前用户消息、附件 ID、Skill ID 和 `request_id`，返回 SSE `delta/error/done` |
| `POST /api/chat/stop` | 按当前用户和 `request_id` 取消生成 |
| `POST /api/chat/attachments/extract` | 上传并解析单个附件，返回解析结果与 `attachment_id` |
| `POST /api/knowledge/documents`、`GET /api/knowledge/documents`、`DELETE /api/knowledge/documents/{id}` | 导入、列出和删除知识库文档 |
| `POST /api/knowledge/documents/{id}/index`、`GET /api/knowledge/index/status` | 异步构建/增量更新索引并查询状态 |
| `GET /api/skills`、`GET /api/skills/{id}` | 获取可见且当前用户有权使用的 Skill 定义 |
| `GET /api/tools`、`POST /api/tools/{tool_id}/execute` | 获取可用 Tool 元数据；仅由对话编排器或受控调试入口执行 Tool |

### 验收标准

1. 首页对话可新建、续聊、流式输出、取消、查看会话历史及查看本轮引用来源。
2. 上述 PDF、DOCX、TXT 和六种图片格式均能通过真实文件上传、解析、附件问答和知识库导入测试；DOC 必须返回明确的不支持提示。
3. 续聊模型上下文只包含当前消息、最近 5 轮摘要、当轮附件预算和 Top-4 RAG 引用，不使用无限历史拼接。
4. RAG 使用 `BAAI/bge-large-zh-v1.5`、1024 维、归一化向量和 FAISS 内积检索；新增文档后能增量更新并立即参与问答。
5. Skill/Tool 的未授权调用、无效参数、超时、跨用户资源访问和试图执行任意命令都被后端拒绝并记录审计日志。
6. 后端能够从 `codex-skill-samples/openai-docs` 导入标准 Codex Skill 包，列表接口可见，首页对话可选择或触发该 Skill，并能证明模型在本轮回复中实际使用了该 Skill 的 `SKILL.md` 指令。
